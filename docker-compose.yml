services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: shortener_db
      POSTGRES_USER: usuario
      POSTGRES_PASSWORD: senha
    ports:
      - "5432:5432"
    volumes:
      - ./data/db:/var/lib/postgresql/data
